import React from "react";
import ReactDOM from "react-dom/client";
import ZombieSurvival from "./ZombieSurvival.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ZombieSurvival />
  </React.StrictMode>
);